# must be unique in a given SonarQube instance
#sonar.projectKey=org.mycompany.myproject

#sonar.projectName=daskpro

# Path is relative to the sonar-project.properties file. Defaults to .
#sonar.sources=src

#sonar.modules=dask

sonar.projectKey=dask
sonar.sources=.